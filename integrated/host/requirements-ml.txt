# Optional heavyweight ML dependencies for real training runs.
# The MVP works without these because rl/inference.py provides a heuristic policy.
gymnasium>=0.29
stable-baselines3>=2.3
ray[rllib]>=2.9
